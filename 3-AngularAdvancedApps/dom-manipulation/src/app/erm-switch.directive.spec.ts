import { ErmSwitchDirective } from './erm-switch.directive';

describe('ErmSwitchDirective', () => {
  it('should create an instance', () => {
    const directive = new ErmSwitchDirective();
    expect(directive).toBeTruthy();
  });
});
