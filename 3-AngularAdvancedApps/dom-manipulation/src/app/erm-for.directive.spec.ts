import { ErmForDirective } from './erm-for.directive';

describe('ErmForDirective', () => {
  it('should create an instance', () => {
    const directive = new ErmForDirective();
    expect(directive).toBeTruthy();
  });
});
